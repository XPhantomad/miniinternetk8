apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
  name: bridge-base-zuri-conf
spec:
  config: '{
    "cniVersion": "0.3.1",
    "name": "bridge-base-zuri",
    "type": "bridge",
    "bridge": "mynet1",
    "ipam": {}
}'