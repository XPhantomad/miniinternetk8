apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
  name: bridge-s4-gene-conf
spec:
  config: '{
    "cniVersion": "0.3.1",
    "name": "bridge-s4-gene",
    "type": "bridge",
    "bridge": "mynet0",
    "ipam": {}
}'