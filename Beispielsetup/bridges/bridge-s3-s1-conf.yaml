apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
  name: bridge-s3-s1-conf
spec:
  config: '{
    "cniVersion": "0.3.1",
    "name": "bridge-s3-s1",
    "type": "bridge",
    "bridge": "mynet1",
    "ipam": {}
}'